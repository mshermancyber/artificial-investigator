services:
  investigator:
    build: .
    container_name: ai-conversation-investigator
    ports:
      - "80:80"
      - "443:443"
    restart: unless-stopped
    # To use your own TLS certificate, mount them:
    # volumes:
    #   - ./certs/server.crt:/etc/nginx/certs/server.crt:ro
    #   - ./certs/server.key:/etc/nginx/certs/server.key:ro
